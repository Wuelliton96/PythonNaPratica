'''
Modulos padrão:
MOdulos são arquivos .py (que contém codigo python) e servem para expandir
as funcionalidades padrão da linguagem.
veja todos os modulos padrão:
https://docs.python.org/3/py-modindex.html
'''
import random
import sys
import os
import re
from random import randint, random

for i in range(10):
    print(randint(0,10), random())